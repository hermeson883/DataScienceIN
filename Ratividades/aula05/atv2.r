# Criar um programa que calcula a área de um triângulo com base e altura inseridas pelo usuário.

base <- as.double(readline('digite a base: '))
altura <- as.double(readline('digite a altura: '))
area <- (base*altura)/2
print(area)


# area = (base*altura)/2
